---
scope: Product Catalog & Purchase Tracking enhancement (brownfield, existing Flask app)
purpose: build-substrate
altitude: feature (governs the 10 epics in the PRD addendum)
updated: 2026-07-23T17:19
---

- (direction) Inputs: finalized PRD + addendum at planning-artifacts/prds/prd-workshop-inventory-tracking-2026-07-21/ (64 FRs, 11 NFRs, indicative data model, brownfield verification notes, 2 blocking spikes Q1/Q7). project-context.md loaded as persistent facts (authoritative existing conventions)
- (constraint) Brownfield: existing conventions are BINDING and mostly [ADOPTED] — app-factory + storage injection, Storage ABC/StorageResult, service layer (*_service.py), dataclass domain models (app/models.py) separate from ORM (app/database.py) with explicit boundary conversion, Decimal-not-float, Alembic via manage.py, legacy Query API, blueprints main/admin, CSRF on, MariaDB JSON for schemaless
- (constraint) [ADOPTED] Entity pattern: single enhanced ORM model on shared Base (app/database.py), domain logic via @hybrid_property (enum<->String bridging) + @property (value-object bridging); dataclass VALUE OBJECTS in app/models.py for composite fields only. NOT a clean domain/ORM two-layer split (project-context.md oversimplified). Exemplar: InventoryItem
- (constraint) [ADOPTED] Category = materialized-path string + self-referential-by-string precedent (MaterialTaxonomy uses string 'parent' col, comma-sep aliases). M2M precedent = Photo + ItemPhotoAssociation association table w/ UniqueConstraint+Index. Enums single-sourced in models.py
- (constraint) [ADOPTED] Service pattern: mariadb_catalog_service.py copies InventoryService — sessionmaker(bind=engine), per-method session try/except/finally-close, legacy session.query()+and_/desc/func, log_audit_operation on mutate. Services return domain objects/bool/None; ROUTES build the {success,...} JSON envelope. StorageResult exists but is not the dominant service return type
- (constraint) [ADOPTED] REST: blueprint app/main/routes.py, _get_storage_backend() reads current_app.config['STORAGE_BACKEND'], every JSON API route @csrf.exempt, envelope {success:bool,...}+status. field-suggestions source query in InventoryService.get_field_value_suggestions gated by FIELD_SUGGESTION_COLUMNS whitelist. api_client.py (requests-only) needs a matching method+frozen result dataclass per new programmatic endpoint (NFR10)
- (constraint) [ADOPTED] Label: label_printer.py LABEL_TYPES dict is single source of truth (already has Sato 1x2/2x4/4x6 +Flag); raster BytesIO -> LpPrinter.print_images() submission seam is barcode-agnostic and REUSED unchanged; new generator branch plugs in at the generator-selection seam (~label_printer.py:108-125). Test short-circuit on TESTING/DISABLE_LABEL_PRINTING
- (constraint) [ADOPTED] Pure utils: app/utils/ (location_validator.py precedent) = module-level pure fns + regex consts + single-source-of-truth docstring, no Flask/DB imports. New gtin.py / internal_id.py / scan_router.py follow this. scan_router mirrors client-side classifyInput() in inventory-move.js (keyboard-wedge: input-event + scannerTimeout, trailing-Enter-wins)
- (constraint) [ADOPTED] Migrations: raw Alembic via manage.py db (migrate/upgrade/downgrade); versions/<12hex>_<slug>.py; new down_revision='8213852b0b94' (current HEAD). Stack pins: Flask 3.1.3, SQLAlchemy 2.0.51, Alembic 1.18.5, PyMySQL 1.2.0, Pillow>=12.3.0, PyMuPDF 1.28.0, pt-p710bt-label-maker(git). MariaDB 11.8
- (decision) JSON columns: adopt MariaDB JSON for Product.attributes (Specifications) per PRD NFR2, even though existing code uses ZERO JSON columns (comma-sep Text elsewhere). Binds: schemaless specs. Prevents: comma-sep-Text hacks that can't represent arbitrary key/value specs. New-to-codebase but PRD-sanctioned
- (question) Genuine forks remaining for Jason: (1) 2D DataMatrix render library incl. system-dep weight [web-research in progress]; (2) Attachment storage: BLOB-in-DB (matches Photo precedent, backed up with DB) vs filesystem-path (PRD Q2 assumption); (3) FK strategy: numeric surrogate-PK FKs vs business-key(internal_id/ja_id-style) FKs per Photo precedent
- (version) Web-verified (2026-07-22) 2D lib: pyStrich 0.18 (2026-07-20) — pure-Python, no system dep (ideal for Pi/ARM64 Docker), native GS1 DataMatrix w/ FNC1-first (added 0.15), PIL output, Py3.13 ok. Fallback: treepoem 3.28 = BWIPP gs1datamatrix gold standard but needs Ghostscript binary. Ruled out: pylibdmtx (decode/no FNC1), ppf-datamatrix (stale/SVG-only/no GS1), segno (QR only), python-barcode (1D). Caveat: pyStrich GS1 is ~6wk old -> scan-test in Q7 spike
- (decision) FORK-A RESOLVED: 2D render lib = pyStrich[png] (pure-Python, no Pi/Docker system dep, native GS1 DataMatrix FNC1-first, PIL output). Q7 spike scan-tests a printed sample (GS1 feature ~6wk old); treepoem+Ghostscript documented fallback
- (decision) FORK-B RESOLVED: attachments = BLOB-in-DB (LONGBLOB), matching Photo precedent; single backup domain; exactly-one-owner (product XOR purchase) invariant enforced. Resolves PRD Q2
- (decision) FORK-C RESOLVED: integer surrogate-PK FKs across catalog tables; products.internal_id is a UNIQUE business key for URLs/labels/scan lookup. Mild divergence from ja_id-keyed Photo precedent, chosen for the richer relationship graph
- (event) All load-bearing decisions captured; distilling spine
- (event) Reviewer gate: lint 0; version-check PASS (0 fail, pyStrich 0.18 + all pins verified, HEAD 8213852b0b94 confirmed); rubric STRONG (0C/0H/1M/3L); adversarial-seams 2C/3H/5M. All findings determinate + intent-aligned -> applying (Finalize gate)
- (decision) AD-5 reworded: scan_router is pure CLASSIFY-only; malformed format-06 -> free_text raw (satisfies NFR8, no raise). AD-15 NEW scan-seam contract: frozen ScanClassification (router) + ScanResolution (service resolve_scan does DB lookup + FR36 fallthrough). Closes C-1 self-contradiction (AD-4 pure vs AD-5 lookup)
- (decision) AD-16 NEW: AI-96/WIT/FNC1 grammar single-sourced in gs1.py (encode+decode, decode absorbs FR37a FNC1 tolerance); scan_router delegates; one config pair GS1_INTERNAL_AI/GS1_INTERNAL_TOKEN read in service, passed to both pure fns, no literal defaults -> FR12c mechanically guaranteed. Closes H-1
- (decision) AD-17 NEW: single free-text search entrypoint = mariadb_catalog_service.search_products(); both Epic-8 search and AD-15 scan fallthrough call it. Mechanism (LIKE/FULLTEXT/rank) deferred, entrypoint fixed. Closes rubric F1 (mis-deferred shared seam)
- (decision) AD-9 rewritten (C-2/H-3/M-5): de-dup scope per type — GTIN/INTERNAL global, VENDOR_SKU/ASIN/FNSKU vendor-scoped; ASIN match = confirm-not-merge when mfr/MPN differ (guards Amazon ASIN reuse); indexing ASIN already on another product rejected+surfaced; UNIQUE(type,value)+UNIQUE(request_key) caught as domain errors; request_key UNIQUE owned by schema; idempotent unit = whole capture txn (Product+Purchase atomic)
- (decision) AD-6 tightened (H-2/M-4): Effective-Low predicate single-sourced (one hybrid/service method, Epic-8 facet reuses); NULL reorder_threshold => threshold branch false; NEW 3rd derived signal Recently Received = purchase on product OR group sibling with received_date>=today-N (N config). AD-10 tightened: group reorder-line = all members EL AND none On Order AND none Recently Received; suppression via derived signals only, never sibling stored-status write; FR33 clear stays product-scoped
- (decision) AD-8 tightened (M-1): internal_id.py yields CANDIDATE only; create-service is sole writer w/ retry-on-UNIQUE; no DB default; INTERNAL id-row written same txn. AD-11 reworded (F4): generator COMPOSITES DataMatrix+description+provenance into full raster then print_images(). Conventions: fixed error envelope {success:false,error:{code,message,field?}} (M-2); canonical category_path lowercase/slash/no-trailing + segment-boundary prefix via normalize_category_path() (M-3). Enrichment Interface (FR60/61) placed as app/services/product_enrichment.py no-op hook (F3)
- (event) spine finalized
- (event) Q7 RESOLVED (physical spike): pyStrich 0.19 GS1 DataMatrix (payload 96+WIT+id) physically SCANS on the Tera HW0009, payload intact ('96WITTEST0001'). pyStrich LOCKED IN as the 2D renderer (AD-11/AD-16); treepoem+Ghostscript fallback NOT needed. Add pyStrich[png] to requirements.txt in the label epic
- (decision) Q1 RESOLVED (physical spike): Tera HW0009 in DEFAULT config transmits FNC1 STRIPPED and emits NO AIM identifier -> internal-ID payload arrives as the bare string '96WITTEST0001' (AI 96 + WIT token + id, no 0x1D, no ]d1). Consequence for AD-5/AD-16/FR36: gs1.decode() + scan_router recognize the internal route STRUCTURALLY on the literal AI+token prefix '96WIT' (self-identifying per FR12a), with no FNC1/AIM dependence. Keep FR37a tolerance (GS/substitute/stripped) in code for safety, but 'stripped' is the deployed reality (simplest of the three)
- (note) Edge noted (non-blocking): a manufacturer's own GS1 DataMatrix carrying AI 01 would arrive FNC1-stripped as '01<gtin>...' (16+ digits), not matching FR36 rule-3 (8/12-14 digits) nor the 96WIT prefix -> falls to free-text search (NFR8 graceful). Uncommon in this shop (retail 1D GTINs + DigiKey ECIA dominate); acceptable
- (decision) AIM symbology emission: DO NOT enable on the Tera HW0009. It is global — would prefix EVERY scan (]C0/]A0/…) and break existing metal-stock JA-ID/location parsing (classifyInput in inventory-move.js) unless all existing parsers are taught to strip AIM prefixes. Unnecessary: structural routing on the bare '96WIT' prefix already works (spike-proven). Keep scanner in default config; AD-5 structural inspection is the primary path, FR37 AIM fast-path stays optional/unused
- (event) Spine re-distilled: Q1/Q7 moved out of Deferred to RESOLVED; AD-16 gains the deployed-reality decode note (96WIT prefix, FNC1 stripped, no AIM); updated 2026-07-23
