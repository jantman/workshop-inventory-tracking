---
topic: Product Catalog & Purchase Tracking enhancement (brownfield) — spec adopting finalized PRD + architecture spine
updated: 2026-07-23T16:31
---

- (direction) Create from structured upstream artifacts (PRD+addendum finalized, architecture spine final/17 ADs). Trust authored separation: distill 5-field kernel; adopt PRD, addendum, spine, project-context as COMPANIONS (not sources) — downstream still reads all four for FR detail, epic decomposition, ADs, existing conventions. Slug=product-catalog
- (decision) Companions (all adopted, bmad-spec does NOT edit): ../../planning-artifacts/prds/prd-...-2026-07-21/prd.md (FRs/journeys/glossary), .../addendum.md (epic decomposition, ECIA+encoding reference, indicative data model, brownfield notes), ../../planning-artifacts/architecture/architecture-...-2026-07-22/ARCHITECTURE-SPINE.md (AD-1..AD-17, conventions, stack), ../../project-context.md (existing app conventions). No sources[] — PRD not fully absorbed (FR detail stays downstream-relevant). Diagrams live in the spine companion; kernel is prose-only
- (note) AD IDs AD-1..AD-17 are stable and cited by capabilities; downstream stories cite them + FR ids. Capability->AD/FR traceability carried inline in each CAP
- (capability) CAP-1 Product & Purchase catalog — intent: record Products (distinct from Purchases) w/ label description, Specifications, identifiers, full purchase history, creatable from a label description alone. success: Product persists w/ 0 purchases & only a description; 3 purchases/2 vendors show chronological history w/ last-paid distinguished. [PRD FR1-6,18-22; AD-1,2,3]
- (capability) CAP-2 Identifier system & internal encoding — intent: reach a Product by any identifier on it/packaging/vendor label, w/ a collision-proof generated internal id. success: GTIN-8/UPC-A/EAN-13/GTIN-14 of one product resolve identically; bad check digit quarantined as GTIN_UNVALIDATED & cannot block a real GTIN; each product gets a GS1 AI-96 'WIT' DataMatrix that cannot collide w/ a GTIN or ECIA payload. [PRD FR7-12d; AD-4,7,8,16]
- (capability) CAP-3 Taxonomy & tagging — intent: classify Products into an accreting materialized-path category tree + cross-cutting tags, created inline. success: typing a new path creates+assigns it; renaming a segment moves descendants (sibling collision rejected); tag filter returns the right subset independent of category. [PRD FR13-17; AD-4]
- (capability) CAP-4 Scan routing & ECIA parsing — intent: any shop barcode resolves correctly from one keyboard-wedge scan. success: internal-ID / ECIA format-06 / GTIN(8|12-14) / free-text each route to the right handler; DigiKey DataMatrix yields P,1P,Q,K,1K,9D; malformed envelope surfaces raw (never errors); unknown scan lands on a pre-filled create form. [PRD FR35-41; AD-5,15,16]
- (capability) CAP-5 Optional stock tracking & reorder signals — intent: track quantity/location only where worthwhile, w/ honest staleness, and one reorder view. success: tri-state quantity renders 3 distinct strings; Effective Low derives from qty<=threshold WITHOUT writing status; receipt clears a manual low w/o flip-flop or touching quantity; reorder view unifies manual+derived w/ on-order marked. [PRD FR23-34; AD-6]
- (capability) CAP-6 Label generation & printing — intent: generate durable 2x4/1x2 labels from records via the existing lp print path, reprintable, dual-encoded. success: label renders description+provenance+GS1 DataMatrix+human-readable id via the existing print_images seam, deterministic, reprintable w/ no re-entry; never emits a 4x6 catalog label. [PRD FR42-47; AD-8,11,16]
- (capability) CAP-7 Order-time capture — intent: capture vendor/SKU/title/price at order time via an Amazon-only bookmarklet + REST, de-duping to existing Products, idempotent. success: a capture creates product+purchase or attaches on identifier/vendor-SKU match; repeat ASIN does not duplicate; same request_key returns the original record; capture-time price recorded as paid, order date defaults to capture date. [PRD FR48-51,49a; AD-9,13]
- (capability) CAP-8 Equivalent products — intent: declare same-part brand-relabels as one group for reorder/price comparison. success: adding B to A's group makes both symmetric members (cross-group add rejected); a group collapses to one reorder line; an inbound order/recent-receipt on any member suppresses the group. [PRD FR62-64; AD-10]
- (capability) CAP-9 Search & retrieval — intent: full-text search across catalog text+identifiers, faceted + bookmarkable, direct-URL product views. success: a term in description/notes/MPN/identifier returns the product; facet by category-prefix/tag/stock-status narrows w/ a bookmarkable URL; internal-id URL opens the product. [PRD FR52-55; AD-17]
- (capability) CAP-10 Attachments — intent: attach datasheets/diagrams/photos to a Product or a Purchase. success: a PDF on a product is retrievable from the product view; every attachment has exactly one owner (product XOR purchase). [PRD FR5; AD-12]
- (capability) CAP-11 Handheld & touch readiness — intent: every action reachable by touch; scan-result view self-sufficient at handheld width; in-progress form state survives network drops. success: at 360px a resolved scan shows identity/Specifications/location/status & allows qty+status inline; a composed description survives a dropped+restored connection; no hover/keyboard dependency. [PRD FR56-59; AD-15]
- (capability) CAP-12 Extensibility hooks — intent: product enrichment behind one no-op interface; creation requires no backfill-hostile field. success: creation invokes the Enrichment Interface (no-op returns empty, creation unchanged); every required creation field is obtainable for a pre-existing item w/ no order. [PRD FR60-61; AD-2,13]
- (constraint) Delivered inside the existing Flask app; NO new services/containers; ships in the existing Docker image on the Raspberry Pi 5, sharing MariaDB/auth/backups (NFR1)
- (constraint) Existing metal-stock functionality unaffected; shared schema not restructured; shared vocabularies (location/vendor) reused by extending field-suggestions, not forked (NFR9). All schema via Alembic from HEAD 8213852b0b94; no create_all (NFR3, AD-14)
- (constraint) Schemaless Specifications use MariaDB JSON (NFR2); no new datastore. Identifier normalization/encoding/routing are pure, exhaustively-tested functions (NFR7, AD-4). REST stays consumable by the requests-only client w/ no added deps (NFR10)
- (constraint) Labels: direct-thermal 2x4/1x2 only (4x6 reserved for shipping); dual barcode+human-readable encoding; deterministic rendering (NFR11). 2D GS1 DataMatrix via pyStrich (pure-Python, no system dep); treepoem+Ghostscript fallback
- (constraint) Single responsive Bootstrap 5.3.2 codebase 360px->21.5in (NFR4). No LLM generation of label copy/specs — read/search path only (RISK-1). Architecture ADs AD-1..AD-17 are binding; downstream stories cite them by stable ID
- (decision) Non-goals (PRD NG1-11 + MVP out-of-scope): no bulk-backfill tooling/importer/CSV/bulk-UI (ad-hoc manual entry works); no mandatory quantity tracking / consumption logging; not replacing Trello, no carrier tracking; no automated Amazon scraping; no LLM label copy; no NoSQL/document store; no vendor-API enrichment (interface is no-op only); no separate mobile UI codebase; no offline persistence/sync; no user-defined part-numbering scheme; no substitutability graph (same-part equivalence only); 4x6 template + multi-vendor bookmarklet out of scope
- (decision) Success signal (PRD SM-1..4): I use it weekly & don't abandon it; every label-worthy item acquired post-launch gets a durable record before shelving (not hand-transcribed) & is identifiable from its physical label alone — offline, no live vendor page — via one scan that resolves to the right record. Counter-metrics: do NOT chase backfill completeness or broad quantity-tracking coverage
- (assumption) 'Label-worthy' ~ the ~half of received items that today get a hand-printed label; distributor-labeled items are catalogued for search/reorder but need no printed label (PRD 2.2)
- (assumption) Order-time capture happens at/near ordering, so capture-time listing price ~ price paid; order date defaults to capture date; both editable (PRD RISK-6)
- (assumption) The existing field-suggestions autocomplete source is extended to include Product locations so the location/vendor vocabulary is bidirectional (PRD FR27; today metal-stock rows only)
- (question) Q1 (BLOCKING SPIKE, gates CAP-4/scan routing): does the Tera HW0009 round-trip a GS1 DataMatrix 'WIT' payload recognizably (data chars intact; FNC1 as GS/substitute/stripped; AIM ]d1)? Resolve before building the internal-ID scan path
- (question) Q7 (BLOCKING SPIKE, gates CAP-6/labels): does pyStrich's ~6-week-old GS1 output scan/verify as a valid GS1 DataMatrix on a real printed sample? Fallback treepoem+Ghostscript. Resolve before label work
- (note) Why = pain + vision: identification-not-inventory. Backdrop: today product metadata is re-transcribed from vanishing vendor pages at unboxing and discarded as data; single-operator hobby workshop; brownfield enhancement
- (event) Self-validate Pass 1 (Coherence, Spec Law 1-6,8): PASS. All 12 CAPs have intent+success; intents are WHAT (HOW lives in cited ADs/companions); all 12+ constraints rule something out; 12 non-goals; success signal is demonstrable (weekly use + durable-record-before-shelving + offline-label-identifiable via one scan); CAP-1..12 unique/stable; lean prose
- (event) Self-validate Pass 2 (Preservation): PASS. All 64 FRs cited across CAP-1..12; NFR1/2/3/4/7/9/10/11 -> Constraints, NFR5 -> CAP-5/AD-6, NFR6 (test coverage) -> spine Testing convention (adopted companion), NFR8 -> CAP-4 success/AD-5; NG1-11+RISK-1 -> Non-goals; SM-1..4 -> Success signal; UJ-1..5 + glossary -> PRD companion; AD-1..17 -> spine companion; assumptions/Q1/Q7 carried; Q2 resolved (BLOB-in-DB) so not open. Wrapper-only dropped: PRD S0 Document Purpose, section numbering, 'working title' notes
- (event) SPEC finalized: SPEC-product-catalog, 12 capabilities, 4 adopted companions (prd.md, addendum.md, ARCHITECTURE-SPINE.md, project-context.md), 0 sources, 3 assumptions, 2 open questions (Q1/Q7 blocking spikes)
